class Solution {
public:
    vector<int> getConcatenation(vector<int>& nums) {

        vector<int> final;

        for(auto ele : nums){
            final.push_back(ele);
        }

        for(auto ele : nums)
        {
            final.push_back(ele);
        }

        return final;
    }
};
