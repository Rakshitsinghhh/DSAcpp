class Solution {
public:
    int numOfUnplacedFruits(vector<int>& fruits, vector<int>& baskets) {
        for(int i=0;i<fruits.size();i++){
            for(int j=0;j<baskets.size();j++){
                if(baskets[j]>=fruits[i]){
                    baskets[j]=0;
                    break;
                }
            }
        }
        int count=0;
        for(auto& ele : baskets){
            if(ele!=0){
                count+=1;
            }
        }
        return count;
        
    }
};
